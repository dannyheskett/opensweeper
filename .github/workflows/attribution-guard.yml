name: attribution-guard

# Thin caller. All logic lives in dannyheskett/.github, shared by every repo.
# Check name in this repo is "guard / guard".
on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  guard:
    uses: dannyheskett/.github/.github/workflows/attribution-guard.yml@v2
    permissions:
      contents: write
