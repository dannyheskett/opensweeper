<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal NativeActivity manifest: the whole game is native C, so there is no
  application Java/Kotlin code beyond the immersive-mode Activity below. Android
  loads libopensweeper.so directly via android.app.NativeActivity (the
  "android.app.lib_name" meta-data names the .so without its "lib" prefix or
  ".so" suffix). Rotates with the device (the grid is re-fitted to the live
  screen every frame); fullscreen with no title bar.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.danheskett.opensweeper">

    <!-- versionCode/versionName are intentionally absent: aapt/aapt2
         - -version-code/- -version-name only inject when the manifest has none,
         so hardcoding them here would silently pin every Play upload to 1. -->
    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="36" />

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application android:label="opensweeper"
                 android:icon="@mipmap/ic_launcher"
                 android:hasCode="true"
                 android:allowBackup="false">

        <activity android:name="com.danheskett.opensweeper.OpensweeperActivity"
                  android:label="opensweeper"
                  android:screenOrientation="fullUser"
                  android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|smallestScreenSize"
                  android:exported="true"
                  android:theme="@style/OpensweeperTheme">

            <!-- Names the native library (libopensweeper.so) NativeActivity loads. -->
            <meta-data android:name="android.app.lib_name" android:value="opensweeper" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
